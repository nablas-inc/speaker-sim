services:
  app_run:
    image: ss-app:latest
    build:
      context: .
    stdin_open: true
    tty: true
    ports:
      - '8501:8501'
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
